# -------------------------------------------------------
# .env.example – Credentials für Moodle-Migration
# Kopieren zu .env und Werte anpassen:
#   cp .env.example .env
# .env ist in .gitignore eingetragen → wird nie committed
# -------------------------------------------------------

DB_ROOT_PASS=changeme_root
DB_NAME=moodle
DB_USER=moodle
DB_PASS=changeme_moodle
